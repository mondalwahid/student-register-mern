{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd student-register && npm run build",
    "install-client": "cd student-register && npm install",
    "start": "node index.js",
    "heroku-postbuild": "cd student-register && npm install --only=dev && npm install && npm run build"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.1",
    "cors": "^2.8.5",
    "dotenv": "^14.3.2",
    "express": "^4.17.2",
    "mongoose": "^6.1.8"
  },
  "engines": {
    "node": "16.4.0"
}
}
